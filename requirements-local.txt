# Core app dependencies
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
pydantic>=2.8.0,<3.0
streamlit>=1.35.0
requests>=2.32.0
numpy>=1.26.0

# Optional local ML dependencies. These are not required for startup
# and are intentionally left out because they can fail on Windows/Python 3.14
# unless a compatible toolchain and a supported Python version are installed.
# Install them only on a machine with a compatible environment:
#   pip install torch transformers sentence-transformers scikit-learn huggingface-hub
