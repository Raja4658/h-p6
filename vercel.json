{
  "runtime": "python@3.11",
  "buildCommand": "pip install -r requirements-vercel.txt",
  "outputDirectory": ".",
  "env": {
    "HUGGINGFACE_API_TOKEN": {
      "required": true
    }
  }
}

